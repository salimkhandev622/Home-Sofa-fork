there is still a problem that why the home page image get disappers when when i make any change in the admin dashboard like chaning any name or text then that home page image get disapper and only the blue background get remain there


I'll investigate the hero image disappearance issue when making changes in the admin dashboard. Let me check the hero slides data and the sync mechanism.

SearchedsaveHeroSlide in D:\Downloads\Home-Sofa\admin\assets\js\dashboard.js






continue


Read
content.txt
Read
dashboard.js
#L1280-1309
Read
dashboard.html
#L540-569
I found the issue! When editing hero slides in the admin dashboard, the image field is not being populated in the form, and there's no image preview. This causes the image to be lost when saving changes. Let me fix this:

dashboard.js
Errored
Read
dashboard.js
#L1207-1246
dashboard.js
+8
                document.getElementById('heroActive').checked = slide.active;
 
                // Show current image preview
                const heroImagePreview = document.getElementById('heroImagePreview');
                const imagePath = slide.image.startsWith('http')
                    ? slide.image
                    : `../public/${slide.image}`;
                heroImagePreview.innerHTML = `<img src="${imagePath}" style="width: 100px; height: 100px; object-fit: cover; border-radius: 4px;">`;
            }
4 hidden lines
            document.getElementById('heroActive').checked = true;
            document.getElementById('heroImagePreview').innerHTML = '';
        }

dashboard.js
+3
                heroData.secondaryButtonLink = heroData.secondaryButtonLink || state.heroSlides[index].secondaryButtonLink || 'phone';
 
                // Ensure ID is preserved
                heroData.id = state.heroSlides[index].id;